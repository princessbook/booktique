import React from 'react';

const ReadBookLayout = ({ children }: { children: React.ReactNode }) => {
  return <div className='h-full   bg-[#EDEEF2]'>{children}</div>;
};

export default ReadBookLayout;
